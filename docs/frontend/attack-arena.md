# Attack Arena UI

Attack Arena uses curated demo text and submits it to the real Phase 4 gateway.

Labels:

- deterministic demo for score story;
- real detector decision;
- real persisted decision/audit;
- not a measured score-integrity benchmark.

Payloads are rendered as text only. No unsafe HTML rendering is used.

