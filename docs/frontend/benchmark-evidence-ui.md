# Benchmark and Evidence UI

The benchmark/evidence surface displays:

- approved Phase 3 run ID;
- generic measured metrics;
- IELTS LOW_SUPPORT warning;
- score integrity NOT_MEASURED;
- deterministic demo separation;
- dataset/run metadata;
- safe failure-explorer metadata.

It does not expose private holdout raw text.

