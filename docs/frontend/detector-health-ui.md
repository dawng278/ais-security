# Detector Health UI

Detector health uses latest persisted decision and runtime readiness data.

Embedding is shown as unavailable/degraded when backend reports missing optional dependency or model. It is not displayed as healthy merely because the detector is configured.

