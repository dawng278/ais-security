# Integration and Runtime UI

The runtime UI shows:

- readiness;
- database state;
- audit persistence state;
- embedding truth state;
- operating mode and version;
- guarded mode transition controls.

It explicitly labels:

- pilot-local SQLite;
- pilot-local rate limiting;
- external IdP pending;
- production readiness NOT_READY.

