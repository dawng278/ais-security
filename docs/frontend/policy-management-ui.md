# Policy Management UI

Backed by policy version APIs.

Implemented:

- list versions;
- create validated draft;
- publish;
- rollback;
- read-only JSON preview;
- role and server-side authorization handling.

No arbitrary code editor or eval-like policy scripting is exposed.

