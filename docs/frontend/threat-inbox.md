# Threat Inbox UI

Backed by `/api/v1/security/incidents`.

Implemented:

- severity/status filtering;
- accessible table;
- safe redacted summary only;
- incident detail links;
- empty/error/degraded states via shared state panel.

Not implemented:

- fake bulk mutation;
- saved filters with backend persistence.

